import { CalendarDays, ChevronRight, Search, Trophy } from "lucide-react";
import { useState } from "react";
import { Input } from "@/components/ui/input";
import { recentGames } from "@/lib/game-data";
export default function History() { const [query, setQuery] = useState(""); const filtered = recentGames.filter((game) => game.opponent.toLowerCase().includes(query.toLowerCase())); return <div className="space-y-8"><div><p className="text-sm font-semibold text-primary">Your record</p><h1 className="mt-2 text-3xl font-extrabold tracking-tight">Match history</h1><p className="mt-2 text-muted-foreground">Look back at your best games and close finishes.</p></div><div className="grid gap-4 sm:grid-cols-3"><Metric label="Matches played" value="24" /><Metric label="Wins" value="16" /><Metric label="Average score" value="348" /></div><div className="rounded-2xl border border-border bg-card shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-border p-5"><div><h2 className="font-bold">Completed matches</h2><p className="mt-1 text-xs text-muted-foreground">24 total games</p></div><div className="relative w-full sm:w-64"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground" /><Input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search opponents" className="h-9 rounded-lg pl-9" /></div></div><div className="divide-y divide-border">{filtered.map((game) => <div key={game.opponent} className="flex flex-wrap items-center gap-4 p-5 hover:bg-muted/40"><div className={`grid h-10 w-10 place-items-center rounded-full ${game.result === "Won" ? "bg-emerald-100 text-emerald-700" : "bg-rose-100 text-rose-600"}`}><Trophy className="h-4 w-4" /></div><div className="min-w-[130px] flex-1"><p className="font-semibold">{game.opponent}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><CalendarDays className="h-3 w-3" /> {game.date}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${game.result === "Won" ? "bg-emerald-100 text-emerald-700" : "bg-rose-100 text-rose-600"}`}>{game.result}</span><span className="w-24 text-right text-sm font-bold">{game.score}</span><button className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground" aria-label={`View ${game.opponent}`}><ChevronRight className="h-4 w-4" /></button></div>)}{!filtered.length && <div className="p-10 text-center text-sm text-muted-foreground">No matches found.</div>}</div></div></div> }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-extrabold">{value}</p></div> }
